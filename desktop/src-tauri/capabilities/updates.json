{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "updates",
	"description": "Auto-update IPC (check/download/install + relaunch) for the SPA, granted regardless of Local/Remote origin classification — the packaged app serves the SPA from the loopback sidecar origin, which Tauri classifies as remote.",
	"windows": ["main"],
	"local": true,
	"remote": {
		"urls": ["http://localhost:4817", "http://localhost:4273"]
	},
	"permissions": ["updater:default", "process:allow-restart"]
}
