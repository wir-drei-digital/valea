# Cancellation Policy

Sessions can be rescheduled or cancelled with at least 24 hours' notice at
no charge.

- Cancellations with less than 24 hours' notice use one session from the
  package.
- If a client is dealing with an emergency, use judgment rather than
  applying the policy strictly — flag it for review instead of deciding
  automatically.
