# Session Brief Writer

Reads a client's ICM page and recent session history, then writes a short
prep brief covering open commitments and useful context for the upcoming
session. Used by the `session_prep_brief` workflow.
